# Ignore test folders (anything starting with "test")
test*/

# Ignore data and storage folders
data/
store/

# Python-related
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
.venv311/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Environment variables
.env
.env.local
.cache/

# OS
.DS_Store
.AppleDouble
.LSOverride

# Poetry
poetry.lock
.data/
.context/

# Benchmark / core2 engine build artifacts (written to the repo root at runtime,
# named after the engine). These are regenerated on every run — never commit them.
/benchmark_llm/
/two_tower/
/gbdt_hist_gbdt/
/context/
/context.index
/datasets/
# per-config LLM caches + shared dataset are reproducible; keep committed results
# curated (report.md / metrics.json / leaderboard.md) but ignore the bulky rebuildables
benchmark/results/shared/
benchmark/results/configs/